[PURPOSE]: "This file references the script needed to use app properly": 

```sh
pip install reactpy asyncio flask json
pip list | grep "json"
pip install flask_cors
pip install flask_sock
pip install flask_cors
pip install dotenv


```

